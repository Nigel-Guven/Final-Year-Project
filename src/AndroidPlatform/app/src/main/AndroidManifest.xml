<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.activities">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="classes.ApplicationChannels"
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:hardwareAccelerated="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:windowSoftInputMode="adjustPan">
        <activity android:name=".SearchReturnActivity"></activity>
        <activity android:name=".PairChatActivity" />
        <activity android:name=".GroupActivity" />
        <activity android:name=".ViewProfileActivity" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".PostDetailsActivity"
            android:label="@string/title_activity_post_details" />
        <activity android:name=".DateTimeActivity" />
        <activity
            android:name=".MapSelectRouteActivity"
            android:label="@string/title_activity_map_select_route" />
        <activity android:name=".RateUsActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".LogOutActivity" />
        <activity android:name=".UserStatisticsActivity" />
        <activity android:name=".ForumActivity" />
        <activity android:name=".DashboardActivity" />
        <activity android:name=".DriverRegistrationActivity" />
        <activity android:name=".DriverLicenseActivity" />
        <activity android:name=".RegistryChooseActivity" />
        <activity android:name=".ProfilePhotoActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".PhoneAddressActivity" />
        <activity android:name=".DobActivity" />
        <activity android:name=".RegisterNameActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".HomeActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
        <meta-data
            android:name="com.google.firebase.ml.vision.DEPENDENCIES"
            android:value="ocr" />
    </application>

</manifest>